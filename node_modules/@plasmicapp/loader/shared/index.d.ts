import * as cli from "./cli";
export declare type PlasmicOpts = {
    dir: string;
    plasmicDir: string;
    pageDir: string;
    projects: string[];
    watch?: boolean;
    initArgs?: cli.initArgs;
    ignorePages?: string[];
};
export declare function generateEntrypoint({ dir, pageDir, projects, watch, plasmicDir, ignorePages, initArgs, }: PlasmicOpts): Promise<void>;
